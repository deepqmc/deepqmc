# version 0.5.0

[train_kwargs]
learning_rate = 0.01
sampler_size = 2_000
n_steps = 10_000
optimizer = "AdamW"
cuda = true
lr_scheduler = "inverse"
decay_rate = 200

[train_kwargs.fit_kwargs]
clip_outliers = true
subbatch_size = 10_000

[train_kwargs.batched_sampler_kwargs]
batch_size = 10_000
sample_every = 100
n_discard = 50
n_decorrelate = 1

[train_kwargs.sampler_kwargs]
n_first_certain = 3
target_acceptance = 0.57
